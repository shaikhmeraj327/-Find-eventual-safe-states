# -Find-eventual-safe-states
Given a directed graph with V vertices numbered from 0 to V-1 and E directed edges,Return all Safe Nodes of A vertex with no outgoing edges is called a terminal node. A vertex is considered safe if every path starting from it eventually reaches a terminal node.the graph.
